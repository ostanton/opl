// Defining a Point structure and using it in a variable
a;5;etc;
test: f32 = 5;

Point: {
    x: f32 = 0;
    y: f32 = 0;
};

p: Point = 1 + -5 / 3 - 2 * -5 - -4;

p: Point = (a: u8) {
    x = 50 * a;
    y = x * 2;
};

cond: bool = p.x / 4 >= (4 - 5) * 2 / 67 or p.y == 2;

// TODO - x and y call parameter list nodes are not children of them!
// They're instead alongside them as children of the local access nodes!
p(5, test, 5 + 2 * 6 / (5 - 2)).x(2).y(aa);

p.x.y().f = 100;

result: u8 = 0;
