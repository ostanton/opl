// Defining a Point structure and using it in a variable

Point: {
    x: f32 = 0;
    y: f32 = 0;
};

p: Point = 1 + -5 / 3 - 2 * -5 - -4;

// value scopes are not implemented yet!
p: Point;// = {
    //x = 50;
    //y = x * 2;
//};

p.x.y = 100;

result: u8 = 0;
